<h1>Flats</h1>


<% @flats.each do |flat| %>
    <h2><%= link_to flat["name"], flat_path(flat["id"]), style: "text-decoration: none;", class: "btn" %></h2>
    <p>€<%= flat["price"] %></p>
<% end %>